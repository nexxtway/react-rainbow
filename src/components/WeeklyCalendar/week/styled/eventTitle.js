import styled from 'styled-components';

const StyledEventTitle = styled.p`
    font-size: 12px;
    margin-top: -1px;
    margin-bottom: -3px;
    font-weight: 900;
`;

export default StyledEventTitle;
