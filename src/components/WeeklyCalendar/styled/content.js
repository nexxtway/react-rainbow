import styled from 'styled-components';

const StyledContent = styled.div`
    overflow: hidden;
    flex: 1 1 auto;
    display: flex;
    flex-direction: row;
    position: relative;
    height: 1200px;
`;
export default StyledContent;
