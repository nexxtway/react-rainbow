import styled from 'styled-components';
import StyledContainer from '../../Avatar/styled/container';

const StyledCounter = styled(StyledContainer)`
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
`;

export default StyledCounter;
