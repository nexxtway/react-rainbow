import wrapTableRows from '../wrapTableRows';

describe('wrapTableRows', () => {
    it('', () => {
        const table = {};
        console.log(JSON.stringify(table));
        wrapTableRows(table);
        console.log(JSON.stringify(table));
    });
});
