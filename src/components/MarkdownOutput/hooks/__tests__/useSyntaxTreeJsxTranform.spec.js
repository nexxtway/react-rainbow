// import { act, renderHook } from '@testing-library/react-hooks';
// import useSyntaxTreeJsxTranform from '../useSyntaxTreeJsxTranform';

describe('useSyntaxTreeJsxTranform', () => {
    it('', () => {
        // const hook = renderHook(value => useSyntaxTreeJsxTranform(value));
        // expect(hook.result.current.isOpen).toBe(false);
    });
});
