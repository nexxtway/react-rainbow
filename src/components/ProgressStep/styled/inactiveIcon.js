import styled from 'styled-components';
import { BORDER_RADIUS_2 } from '../../../styles/borderRadius';

const StyledInactiveIcon = styled.div`
    border-radius: ${BORDER_RADIUS_2};
`;

export default StyledInactiveIcon;
