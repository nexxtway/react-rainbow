import styled from 'styled-components';
import { MARGIN_XX_SMALL } from '../../../styles/margins';

const StyledRightIcon = styled.span`
    margin-left: ${MARGIN_XX_SMALL};
`;

export default StyledRightIcon;
