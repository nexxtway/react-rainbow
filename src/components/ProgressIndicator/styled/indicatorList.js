import styled from 'styled-components';

const StyledIndicatorList = styled.ol`
    padding: 0;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin: auto;
    position: relative;
    z-index: 1;
    box-sizing: border-box;
`;

export default StyledIndicatorList;
