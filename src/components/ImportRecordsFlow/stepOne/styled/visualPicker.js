import styled from 'styled-components';
import VisualPicker from '../../../VisualPicker';

const StyledVisualPicker = styled(VisualPicker)`
    margin: 0 24px 16px 24px;
`;

export default StyledVisualPicker;
