import styled from 'styled-components';
import Button from '../../Button';
import { MARGIN_XX_LARGE } from '../../../styles/margins';

const StyledNextButton = styled(Button)`
    margin-left: ${MARGIN_XX_LARGE};
`;

export default StyledNextButton;
