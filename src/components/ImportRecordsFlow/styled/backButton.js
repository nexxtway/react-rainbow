import styled from 'styled-components';
import Button from '../../Button';
import { MARGIN_XX_LARGE } from '../../../styles/margins';

const StyledBackButton = styled(Button)`
    margin-right: ${MARGIN_XX_LARGE};
`;

export default StyledBackButton;
