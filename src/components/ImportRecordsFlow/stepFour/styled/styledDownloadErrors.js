import styled from 'styled-components';

const StyledDownloadErrors = styled.div`
    color: #000;
    display: flex;
    font-size: 16px;
    width: 45%;

    &:hover {
        cursor: pointer;
    }
`;

export default StyledDownloadErrors;
