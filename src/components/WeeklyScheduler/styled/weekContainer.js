import styled from 'styled-components';

const StyledWeekContainer = styled.div`
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-grow: 1;
    margin-right: 12px;
`;

export default StyledWeekContainer;
