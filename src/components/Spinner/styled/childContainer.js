import styled from 'styled-components';

const StyledChildContainer = styled.span`
    display: inline-flex;
    justify-content: center;
    align-items: center;
    position: absolute;
    flex-direction: column;
`;

export default StyledChildContainer;
